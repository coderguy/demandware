(function (dw, Backbone, $, _, dust) {

  dw.events.ContextEvents = {};
  _.extend(dw.events.ContextEvents, Backbone.Events);

  dw.events.ContextEvents.on("context:set", function () {
    
  });

}(window.app.dw, Backbone, jQuery, _, dust));
  